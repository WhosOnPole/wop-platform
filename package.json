{
  "name": "wop-platform",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install:all": "pnpm install --frozen-lockfile",
    "build": "pnpm -r --filter ./apps/* build",
    "dev:web": "pnpm --filter web dev",
    "dev:admin": "pnpm --filter admin dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "postbuild": "node scripts/postbuild-db-setup.js"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "typescript": "^5.8.3",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}